/*
 * This file was generated by emfts-codegen.
 * DO NOT EDIT - changes will be overwritten.
 *
 * @generated
 */

import { BasicEPackage, BasicEClass, EPackageRegistry } from '@emfts/core';
import type { EClass, EAttribute } from '@emfts/core';
import { SystemactionsFactory } from './SystemactionsFactory.js';

/**
 * Resolve a dependency package from the registry, failing with an actionable
 * message instead of a null dereference when it is not registered yet
 */
function requireEPackage(nsURI: string) {
  const pkg = EPackageRegistry.INSTANCE.getEPackage(nsURI);
  if (!pkg) {
    throw new Error(`EPackage '${nsURI}' is not registered. Access the eINSTANCE of that model's generated package (or register it via EPackageRegistry.INSTANCE.registerPackage) before initializing SystemactionsPackage.`);
  }
  return pkg;
}

/**
 * Systemactions Package
 * @generated
 */
export class SystemactionsPackage extends BasicEPackage {
  static readonly eNAME = 'systemactions';
  static readonly eNS_URI = 'http://org.eclipse.daanse.board.app.lib.events.systemactions';
  static readonly eNS_PREFIX = 'systemactions';

  // Singleton instance
  private static _instance: SystemactionsPackage;

  static get eINSTANCE(): SystemactionsPackage {
    if (!this._instance) {
      this._instance = new SystemactionsPackage();
      this._instance.init();
    }
    return this._instance;
  }

  /**
   * Literals for quick access to metaclasses and features
   */
  static readonly Literals = {
    SYSTEM_ACTIONS: null as unknown as EClass,
    PAGE_ACTIONS: null as unknown as EClass,
  };

  private constructor() {
    super();
    this.setName(SystemactionsPackage.eNAME);
    this.setNsURI(SystemactionsPackage.eNS_URI);
    this.setNsPrefix(SystemactionsPackage.eNS_PREFIX);
  }

  /**
   * Initialize package contents
   */
  private init(): void {
    // Register this package under its nsURI so other generated packages can
    // resolve their cross-package references from the registry (#36).
    // eINSTANCE is already assigned at this point
    EPackageRegistry.INSTANCE.set(SystemactionsPackage.eNS_URI, this);
    // Wire the generated factory so loaded/created instances are typed Impls.
    this.setEFactoryInstance(SystemactionsFactory.eINSTANCE);

    // Create SystemActions class
    const systemActionsClass = new BasicEClass();
    systemActionsClass.setName('SystemActions');
    systemActionsClass.setAbstract(false);
    systemActionsClass.setInterface(false);
    this.getEClassifiers().push(systemActionsClass);
    systemActionsClass.setEPackage(this);
    SystemactionsPackage.Literals.SYSTEM_ACTIONS = systemActionsClass;

    // Create PageActions class
    const pageActionsClass = new BasicEClass();
    pageActionsClass.setName('PageActions');
    pageActionsClass.setAbstract(false);
    pageActionsClass.setInterface(false);
    this.getEClassifiers().push(pageActionsClass);
    pageActionsClass.setEPackage(this);
    SystemactionsPackage.Literals.PAGE_ACTIONS = pageActionsClass;


    // ============================================
    // Set ESuperTypes (must be done after all classes are created)
    // ============================================
    (SystemactionsPackage.Literals.SYSTEM_ACTIONS as BasicEClass).getESuperTypes().push(requireEPackage('http://org.eclipse.daanse.board.app.lib.events').getEClassifier('SystemActionInterface')! as EClass);

    // ============================================
    // Set ETypes for all features (must be done after all classes are created).
    // An EAttribute without eType leaves the XMI reader no EDataType to
    // convert against - every value would arrive as a raw string (#37)
    // ============================================

    // ============================================
    // Register XML name mappings from ExtendedMetaData annotations
    // ============================================
  }
}
