/*
 * This file was generated by emfts-codegen.
 * DO NOT EDIT - changes will be overwritten.
 *
 * @generated
 */

import { BasicEObject } from '@emfts/core';
import { createBasicEList } from '@emfts/core';
import type { EClass, EStructuralFeature, EList } from '@emfts/core';
import type { XMLARequestParams } from './XMLARequestParams.js';
import { XmlastorePackage } from './XmlastorePackage.js';

/**
 * Implementation of XMLARequestParams
 * @generated
 */
export class XMLARequestParamsImpl extends BasicEObject implements XMLARequestParams {
  // Feature ID Constants (eLiterals)
  static readonly ROWS: number = 0;
  static readonly COLUMNS: number = 1;
  static readonly MEASURES: number = 2;
  static readonly FILTERS: number = 3;

  // Private fields
  private _rows!: EList<unknown>;
  private _columns!: EList<unknown>;
  private _measures!: EList<unknown>;
  private _filters!: EList<unknown>;

  /**
   * Returns the EClass of this object
   */
  override eClass(): EClass {
    return XmlastorePackage.Literals.X_M_L_A_REQUEST_PARAMS;
  }

  // Getters and Setters
  get rows(): EList<unknown> {
    if (!this._rows) {
      this._rows = createBasicEList<any>(this, this.eClass().getEStructuralFeature('rows')!);
    }
    return this._rows;
  }

  get columns(): EList<unknown> {
    if (!this._columns) {
      this._columns = createBasicEList<any>(this, this.eClass().getEStructuralFeature('columns')!);
    }
    return this._columns;
  }

  get measures(): EList<unknown> {
    if (!this._measures) {
      this._measures = createBasicEList<any>(this, this.eClass().getEStructuralFeature('measures')!);
    }
    return this._measures;
  }

  get filters(): EList<unknown> {
    if (!this._filters) {
      this._filters = createBasicEList<any>(this, this.eClass().getEStructuralFeature('filters')!);
    }
    return this._filters;
  }

  // Reflective API

  /**
   * Returns the value of the given feature
   */
  override eGet(feature: EStructuralFeature): unknown {
    const featureID = this.eClass().getFeatureID(feature);
    switch (featureID) {
      case XMLARequestParamsImpl.ROWS:
        return this.rows;
      case XMLARequestParamsImpl.COLUMNS:
        return this.columns;
      case XMLARequestParamsImpl.MEASURES:
        return this.measures;
      case XMLARequestParamsImpl.FILTERS:
        return this.filters;
      default:
        return super.eGet(feature);
    }
  }

  /**
   * Sets the value of the given feature
   */
  override eSet(feature: EStructuralFeature, newValue: unknown): void {
    const featureID = this.eClass().getFeatureID(feature);
    switch (featureID) {
      case XMLARequestParamsImpl.ROWS:
        this.rows.clear();
        this.rows.addAll(newValue as any[]);
        super.eSet(feature, newValue);
        break;
      case XMLARequestParamsImpl.COLUMNS:
        this.columns.clear();
        this.columns.addAll(newValue as any[]);
        super.eSet(feature, newValue);
        break;
      case XMLARequestParamsImpl.MEASURES:
        this.measures.clear();
        this.measures.addAll(newValue as any[]);
        super.eSet(feature, newValue);
        break;
      case XMLARequestParamsImpl.FILTERS:
        this.filters.clear();
        this.filters.addAll(newValue as any[]);
        super.eSet(feature, newValue);
        break;
      default:
        super.eSet(feature, newValue);
    }
  }

  /**
   * Returns whether the feature has been set
   */
  override eIsSet(feature: EStructuralFeature): boolean {
    const featureID = this.eClass().getFeatureID(feature);
    switch (featureID) {
      case XMLARequestParamsImpl.ROWS:
        return this._rows !== undefined && !this._rows.isEmpty();
      case XMLARequestParamsImpl.COLUMNS:
        return this._columns !== undefined && !this._columns.isEmpty();
      case XMLARequestParamsImpl.MEASURES:
        return this._measures !== undefined && !this._measures.isEmpty();
      case XMLARequestParamsImpl.FILTERS:
        return this._filters !== undefined && !this._filters.isEmpty();
      default:
        return super.eIsSet(feature);
    }
  }

  /**
   * Unsets the given feature
   */
  override eUnset(feature: EStructuralFeature): void {
    const featureID = this.eClass().getFeatureID(feature);
    switch (featureID) {
      case XMLARequestParamsImpl.ROWS:
        if (this._rows) this._rows.clear();
        return;
      case XMLARequestParamsImpl.COLUMNS:
        if (this._columns) this._columns.clear();
        return;
      case XMLARequestParamsImpl.MEASURES:
        if (this._measures) this._measures.clear();
        return;
      case XMLARequestParamsImpl.FILTERS:
        if (this._filters) this._filters.clear();
        return;
      default:
        super.eUnset(feature);
    }
  }


  /**
   * What this object is when it is stored.
   *
   * The plain names, not the private fields the getters sit in: those
   * are this class's business, and a stored board is read by things
   * that only know the model.
   */
  toJSON(): Record<string, unknown> {
    return {
      rows: this.rows?.toArray?.() ?? this.rows,
      columns: this.columns?.toArray?.() ?? this.columns,
      measures: this.measures?.toArray?.() ?? this.measures,
      filters: this.filters?.toArray?.() ?? this.filters,
    };
  }
}
