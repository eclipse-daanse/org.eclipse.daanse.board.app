/*
 * This file was generated by emfts-codegen.
 * DO NOT EDIT - changes will be overwritten.
 *
 * @generated
 */

import { BasicEPackage, BasicEClass, BasicEAttribute, EPackageRegistry, getEcorePackage } from '@emfts/core';
import type { EClass, EAttribute, EReference } from '@emfts/core';
import { CodesettingsFactory } from './CodesettingsFactory.js';

/**
 * Codesettings Package
 * @generated
 */
export class CodesettingsPackage extends BasicEPackage {
  static readonly eNAME = 'codesettings';
  static readonly eNS_URI = 'http://org.eclipse.daanse.board.app.ui.vue.widget.code';
  static readonly eNS_PREFIX = 'codesettings';

  // Singleton instance
  private static _instance: CodesettingsPackage;

  static get eINSTANCE(): CodesettingsPackage {
    if (!this._instance) {
      this._instance = new CodesettingsPackage();
      this._instance.init();
    }
    return this._instance;
  }

  /**
   * Literals for quick access to metaclasses and features
   */
  static readonly Literals = {
    CODE_SETTINGS: null as unknown as EClass,
    CODE_SETTINGS__CODE: null as unknown as EAttribute | EReference,
    CODE_SETTINGS__THEME: null as unknown as EAttribute | EReference,
    CODE_SETTINGS__LANGUAGE: null as unknown as EAttribute | EReference,
  };

  private constructor() {
    super();
    this.setName(CodesettingsPackage.eNAME);
    this.setNsURI(CodesettingsPackage.eNS_URI);
    this.setNsPrefix(CodesettingsPackage.eNS_PREFIX);
  }

  /**
   * Initialize package contents
   */
  private init(): void {
    // Register this package under its nsURI so other generated packages can
    // resolve their cross-package references from the registry (#36).
    // eINSTANCE is already assigned at this point
    EPackageRegistry.INSTANCE.set(CodesettingsPackage.eNS_URI, this);
    // Wire the generated factory so loaded/created instances are typed Impls.
    this.setEFactoryInstance(CodesettingsFactory.eINSTANCE);

    // Create CodeSettings class
    const codeSettingsClass = new BasicEClass();
    codeSettingsClass.setName('CodeSettings');
    codeSettingsClass.setAbstract(false);
    codeSettingsClass.setInterface(false);
    this.getEClassifiers().push(codeSettingsClass);
    codeSettingsClass.setEPackage(this);
    CodesettingsPackage.Literals.CODE_SETTINGS = codeSettingsClass;

    // Create code feature
    const codeSettings_code = new BasicEAttribute();
    codeSettings_code.setName('code');
    codeSettings_code.setLowerBound(0);
    codeSettings_code.setUpperBound(1);
    codeSettingsClass.getEStructuralFeatures().push(codeSettings_code);
    CodesettingsPackage.Literals.CODE_SETTINGS__CODE = codeSettings_code;

    // Create theme feature
    const codeSettings_theme = new BasicEAttribute();
    codeSettings_theme.setName('theme');
    codeSettings_theme.setLowerBound(0);
    codeSettings_theme.setUpperBound(1);
    codeSettingsClass.getEStructuralFeatures().push(codeSettings_theme);
    CodesettingsPackage.Literals.CODE_SETTINGS__THEME = codeSettings_theme;

    // Create language feature
    const codeSettings_language = new BasicEAttribute();
    codeSettings_language.setName('language');
    codeSettings_language.setLowerBound(0);
    codeSettings_language.setUpperBound(1);
    codeSettingsClass.getEStructuralFeatures().push(codeSettings_language);
    CodesettingsPackage.Literals.CODE_SETTINGS__LANGUAGE = codeSettings_language;


    // ============================================
    // Set ESuperTypes (must be done after all classes are created)
    // ============================================

    // ============================================
    // Set ETypes for all features (must be done after all classes are created).
    // An EAttribute without eType leaves the XMI reader no EDataType to
    // convert against - every value would arrive as a raw string (#37)
    // ============================================
    (CodesettingsPackage.Literals.CODE_SETTINGS__CODE as BasicEAttribute).setEType(getEcorePackage().getEClassifier('EString')!);
    (CodesettingsPackage.Literals.CODE_SETTINGS__THEME as BasicEAttribute).setEType(getEcorePackage().getEClassifier('EString')!);
    (CodesettingsPackage.Literals.CODE_SETTINGS__LANGUAGE as BasicEAttribute).setEType(getEcorePackage().getEClassifier('EString')!);

    // ============================================
    // Register XML name mappings from ExtendedMetaData annotations
    // ============================================
  }
}
