/*
 * This file was generated by emfts-codegen.
 * DO NOT EDIT - changes will be overwritten.
 *
 * @generated
 */

import { BasicEPackage, BasicEClass, BasicEReference, EPackageRegistry } from '@emfts/core';
import type { EClass, EAttribute, EReference } from '@emfts/core';
import { DatatablesettingsFactory } from './DatatablesettingsFactory.js';

/**
 * Resolve a dependency package from the registry, failing with an actionable
 * message instead of a null dereference when it is not registered yet
 */
function requireEPackage(nsURI: string) {
  const pkg = EPackageRegistry.INSTANCE.getEPackage(nsURI);
  if (!pkg) {
    throw new Error(`EPackage '${nsURI}' is not registered. Access the eINSTANCE of that model's generated package (or register it via EPackageRegistry.INSTANCE.registerPackage) before initializing DatatablesettingsPackage.`);
  }
  return pkg;
}

/**
 * Datatablesettings Package
 * @generated
 */
export class DatatablesettingsPackage extends BasicEPackage {
  static readonly eNAME = 'datatablesettings';
  static readonly eNS_URI = 'http://org.eclipse.daanse.board.app.ui.vue.widget.table.data';
  static readonly eNS_PREFIX = 'datatablesettings';

  // Singleton instance
  private static _instance: DatatablesettingsPackage;

  static get eINSTANCE(): DatatablesettingsPackage {
    if (!this._instance) {
      this._instance = new DatatablesettingsPackage();
      this._instance.init();
    }
    return this._instance;
  }

  /**
   * Literals for quick access to metaclasses and features
   */
  static readonly Literals = {
    DATA_TABLE_SETTINGS: null as unknown as EClass,
    DATA_TABLE_SETTINGS__HEADER_BACKGROUND: null as unknown as EAttribute | EReference,
  };

  private constructor() {
    super();
    this.setName(DatatablesettingsPackage.eNAME);
    this.setNsURI(DatatablesettingsPackage.eNS_URI);
    this.setNsPrefix(DatatablesettingsPackage.eNS_PREFIX);
  }

  /**
   * Initialize package contents
   */
  private init(): void {
    // Register this package under its nsURI so other generated packages can
    // resolve their cross-package references from the registry (#36).
    // eINSTANCE is already assigned at this point
    EPackageRegistry.INSTANCE.set(DatatablesettingsPackage.eNS_URI, this);
    // Wire the generated factory so loaded/created instances are typed Impls.
    this.setEFactoryInstance(DatatablesettingsFactory.eINSTANCE);

    // Create DataTableSettings class
    const dataTableSettingsClass = new BasicEClass();
    dataTableSettingsClass.setName('DataTableSettings');
    dataTableSettingsClass.setAbstract(false);
    dataTableSettingsClass.setInterface(false);
    this.getEClassifiers().push(dataTableSettingsClass);
    dataTableSettingsClass.setEPackage(this);
    DatatablesettingsPackage.Literals.DATA_TABLE_SETTINGS = dataTableSettingsClass;

    // Create headerBackground feature
    const dataTableSettings_headerBackground = new BasicEReference();
    dataTableSettings_headerBackground.setContainment(false);
    dataTableSettings_headerBackground.setName('headerBackground');
    dataTableSettings_headerBackground.setLowerBound(0);
    dataTableSettings_headerBackground.setUpperBound(1);
    dataTableSettingsClass.getEStructuralFeatures().push(dataTableSettings_headerBackground);
    DatatablesettingsPackage.Literals.DATA_TABLE_SETTINGS__HEADER_BACKGROUND = dataTableSettings_headerBackground;


    // ============================================
    // Set ESuperTypes (must be done after all classes are created)
    // ============================================

    // ============================================
    // Set ETypes for all features (must be done after all classes are created).
    // An EAttribute without eType leaves the XMI reader no EDataType to
    // convert against - every value would arrive as a raw string (#37)
    // ============================================
    (DatatablesettingsPackage.Literals.DATA_TABLE_SETTINGS__HEADER_BACKGROUND as BasicEReference).setEType(requireEPackage('org.eclipse.daanse.board.app.ui.vue.composables').getEClassifier('VariableWrapper')!);

    // ============================================
    // Register XML name mappings from ExtendedMetaData annotations
    // ============================================
  }
}
